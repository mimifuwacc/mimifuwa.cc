name: Deploy to Vercel Preview

on:
  push:
    ignore-branches:
      - main
      - devel

jobs:
  deploy:
    uses: ./.github/workflows/deploy-vercel.yml
    with:
      environment: 'preview'
      deployment: false
      production: false
      pr_labels: 'deployed(preview)'
