name: Deploy and Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    # dotenvx: GitHub Secrets に載せるのは復号鍵だけ。CLOUDFLARE_* / ADMIN_SECRET /
    # GRAPHQL_URL は暗号化済み .env.production から dotenvx run 経由で注入される。
    env:
      DOTENV_PRIVATE_KEY_PRODUCTION: ${{ secrets.DOTENV_PRIVATE_KEY_PRODUCTION }}
    steps:
      - uses: actions/checkout@v6

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Migrate D1
        working-directory: apps/api
        run: pnpm exec dotenvx run -f ../../.env.production -- pnpm exec wrangler d1 migrations apply mimifuwacc-blogs --remote

      - name: Set API secret (prod)
        working-directory: apps/api
        run: pnpm exec dotenvx run -f ../../.env.production -- sh -c 'echo "$ADMIN_SECRET" | pnpm exec wrangler secret put ADMIN_SECRET'

      - name: Deploy API
        working-directory: apps/api
        run: pnpm exec dotenvx run -f ../../.env.production -- pnpm exec wrangler deploy --config wrangler.toml

      - name: Build web
        working-directory: apps/web
        run: pnpm exec dotenvx run -f ../../.env.production -- pnpm run build

      - name: Deploy web
        working-directory: apps/web
        run: pnpm exec dotenvx run -f ../../.env.production -- pnpm exec wrangler deploy --config wrangler.jsonc

      - name: Build admin
        working-directory: apps/admin
        run: pnpm exec dotenvx run -f ../../.env.production -- pnpm exec opennextjs-cloudflare build

      - name: Set admin secret (prod)
        working-directory: apps/admin
        run: pnpm exec dotenvx run -f ../../.env.production -- sh -c 'echo "$ADMIN_SECRET" | pnpm exec wrangler secret put ADMIN_SECRET --config wrangler.jsonc'

      - name: Deploy admin
        working-directory: apps/admin
        run: pnpm exec dotenvx run -f ../../.env.production -- pnpm exec wrangler deploy --config wrangler.jsonc

  release:
    needs: deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set release tag
        env:
          TZ: "Asia/Tokyo"
        run: |
          echo "RELEASE_TAG=$(date '+%Y-%m-%d-%H%M%S')" >> $GITHUB_ENV
          echo "RELEASE_NAME=$(date '+%Y-%m-%d %H:%M:%S')" >> $GITHUB_ENV

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: ${{ env.RELEASE_NAME }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
