{
  "name": "@mimifuwacc/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --persist-to .wrangler/state --var API_ADMIN_SECRET:local-development",
    "dev:remote": "dotenvx run -f ../../env/.env.devel -- wrangler dev --env devel --remote",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/pages-plugin-vercel-og": "^0.1.2",
    "drizzle-orm": "^0.45.2",
    "effect": "^3.17.0",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "typescript": "catalog:",
    "wrangler": "catalog:"
  }
}
