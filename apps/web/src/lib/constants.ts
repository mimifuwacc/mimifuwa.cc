// アプリ全体で使う定数（マジックナンバーの集約）

/** ナビゲーション進捗バーの完了アニメーションを消すまでの待機時間 */
export const NAV_PROGRESS_DONE_DELAY_MS = 400;

/** コピー完了表示を元に戻すまでの時間 */
export const COPY_FEEDBACK_DURATION_MS = 2000;

/** 目次のスクロール追従でアクティブ判定する上端オフセット（ヘッダー + 余白） */
export const SCROLL_SPY_OFFSET_PX = 120;

/** 記事一覧取得時の最大件数 */
export const POSTS_PAGE_SIZE = 100;

/** トップページに表示する最近の記事数 */
export const RECENT_POSTS_COUNT = 6;

/** カードに表示するタグの最大数 */
export const MAX_VISIBLE_TAGS = 3;
